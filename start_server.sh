#!/bin/bash

# ===================================================
# サーバー起動スクリプト
# 初回のみ下の ANTHROPIC_API_KEY を自分のキーに書き換えてください
# ===================================================

# Railway では環境変数で設定するため、ローカル実行時のみ以下を有効にしてください
# export ANTHROPIC_API_KEY="your-api-key-here"

# サーバー起動
cd "$(dirname "$0")"
python server.py
